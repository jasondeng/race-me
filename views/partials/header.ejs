<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Beat Me</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.1.8/semantic.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
    <body>

    <div class="ui fixed inverted menu">
        <div class="ui container">
            <div class="header item"><i class="big map icon"></i>Beat Me!</div>
            <a class="item" href="/">Home</a>
            <div class="right menu">
                <% if(!currentUser) { %>
                    <a class="item" href="/register">Sign Up</a>
                    <a class="item" href="/login">Login</a>
                <% } else { %>
                    <a class="item" href="#"><%= currentUser.username %></a>
                    <a class="item" href="/logout">Logout</a>
                    <a class="item" href="#">Help</a>
                <% } %>
            </div>
        </div>
    </div>

    <!--<div class="ui main container ">-->
        <!--<% if(error && error.length > 0) { %>-->
        <!--<div class="ui error message" role="alert">-->
            <!--<i class="close icon"></i>-->
            <!--<%= error %>-->
        <!--</div>-->
        <!--<% } %>-->
        <!--<% if(success && success.length > 0) { %>-->
        <!--<div class="ui success message" role="alert">-->
            <!--<i class="close icon"></i>-->
            <!--<%= success %>-->
        <!--</div>-->
        <!--<% } %>-->
    <!--</div>-->

